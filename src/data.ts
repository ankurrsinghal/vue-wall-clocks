export const getting_started = `npm install vue-wall-clocks`;

export const simple = `<template>
  <SimpleWallClock />
</template>

<script setup>
  import { SimpleWallClock } from 'vue-wall-clocks'
</script>
`;